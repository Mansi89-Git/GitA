name: Push Trigger Workflow
on:
 push:
   branches:
     - main

jobs:
  push-job:
    runs-on: ubuntu-latest

    steps:
      - name: Success Message
        run: echo "Push workflow executed successfully!"

      - name: Display Date and Time
        run: date
      - name: Display Current Working Directory
        run: pwd

  
     
         
