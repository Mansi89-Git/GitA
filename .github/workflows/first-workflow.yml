name: First Github Actions Workflow
on:
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Welcome
        run: echo "Welcome to Github Actions!"

    
